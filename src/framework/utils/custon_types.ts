export enum BorderPattern {
    solid = 'solid',
    dotted = 'dotted',
    dashed = 'dashed',
}

export enum StrokeAlign {
    inside = 'inside',
    outside = 'outside',
    center = 'center',
}

export enum DividerLineStyle {
    solid = 'solid',
    dotted = 'dotted',
    dashed = 'dashed',
    dashDotted = 'dashDotted',
}
